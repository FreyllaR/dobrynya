[pytest]
testpaths = tests
addopts = -q --tb=short -p no:cacheprovider --timeout=180
# зависший тест падает с ошибкой, а не висит вечно
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
